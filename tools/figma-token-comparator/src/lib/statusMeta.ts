/**
 * Тексты статусов: название, английский термин, тональность и подсказка.
 *
 * Ключи статусов общие для цветов и типографики (lib/statusKeys.ts), а
 * смысл у части из них разный: «Совпало значение» у цвета — совпал hex, у
 * типографики — совпали все свойства текста. Поэтому у типографики свои
 * тексты там, где базовые говорят о цвете (находка №24). Тональность общая:
 * цвет бейджа означает срочность и от категории не зависит.
 */

import type { TokenCategory } from "../comparators/types";
import type { StatusFilterKey } from "./statusKeys";

/** Тональность бейджа. Цвета тональностей заданы в ui.html, здесь — только выбор. */
export type BadgeTone = "neutral" | "success" | "info" | "warning" | "danger";

export interface StatusMeta {
  /** Название статуса в интерфейсе. */
  label: string;
  /** Английский термин, которым статус называют в команде и в документации. */
  term: string;
  /** Тональность бейджа — единственный источник его цвета. */
  tone: BadgeTone;
  /** Что это значит и что с этим делать. Показывается подсказкой при наведении. */
  hint: string;
}

/**
 * Единственный источник правды по статусам: название, термин, цвет и подсказка.
 * Любое место интерфейса (таблица цветов, таблица типографики, фильтр в шапке)
 * берёт бейдж отсюда — расхождения между ними невозможны по построению.
 */
const STATUS_META: Record<StatusFilterKey, StatusMeta> = {
  exact: {
    label: "Совпадает с библиотекой",
    term: "Exact match",
    tone: "success",
    hint: "Слой уже привязан к этой переменной библиотеки — делать ничего не нужно.",
  },
  mapped: {
    label: "Решение принято",
    term: "Mapped",
    tone: "success",
    hint: "По этой группе решение уже зафиксировано и подтянулось из истории.",
  },
  value: {
    label: "Совпало значение",
    term: "Value match",
    tone: "info",
    hint: "Цвет в точности совпадает со значением токена библиотеки, но слой к токену не привязан.",
  },
  "name-match": {
    label: "Совпало имя",
    term: "Name match",
    tone: "warning",
    hint: "Имя переменной или стиля совпало с токеном библиотеки, но значения умеренно расходятся.",
  },
  "name-match-unresolved": {
    label: "Значение не прочитано",
    term: "Name match (value unknown)",
    tone: "warning",
    hint: "Имя совпало, но значение токена получить не удалось — обычно это ссылка на переменную из другого файла. Сравните вручную.",
  },
  conflict: {
    label: "Конфликт значений",
    term: "Conflict",
    tone: "danger",
    hint: "Имя совпало с токеном библиотеки, но цвет отличается заметно. Обычно в макете осталось устаревшее значение.",
  },
  approximate: {
    label: "Близкое значение",
    term: "Approximate match",
    tone: "warning",
    hint: "Точного совпадения нет, но в библиотеке есть близкий цвет. Чем меньше ΔE, тем ближе оттенок.",
  },
  "name-mismatch": {
    label: "Имя не по системе",
    term: "Name mismatch",
    tone: "warning",
    hint: "Стиль текста применён, но его имя не совпадает с ожидаемым токеном системы.",
  },
  detached: {
    label: "Отвязанная иконка",
    term: "Detached icon",
    tone: "warning",
    hint: "Точная копия иконки библиотеки, но не экземпляр компонента.",
  },
  "mixed-unresolved": {
    label: "Смешанные значения",
    term: "Mixed (needs review)",
    tone: "neutral",
    hint: "Внутри слоя разные шрифты или размеры — сравнить с одним стилем нельзя, нужен ручной разбор.",
  },
  "layout-only": {
    label: "Нет в библиотеке",
    term: "Layout only",
    tone: "neutral",
    hint: "Совпадений в библиотеке не нашлось ни по имени, ни по значению.",
  },
  "style-binding": {
    label: "Стиль вместо токена",
    term: "Style binding",
    tone: "warning",
    hint: "Цвет задан стилем Figma, а не переменной. Автоматически заменить нельзя — нужно ваше решение.",
  },
  "ghost-binding": {
    label: "Потерянный стиль",
    term: "Ghost style",
    tone: "danger",
    hint: "Слой ссылается на стиль, которого больше нет в файле. Цвет виден, но привязка потеряна.",
  },
  "hardcoded-no-analog": {
    label: "Цвет вручную",
    term: "Hardcoded (no analog)",
    tone: "neutral",
    hint: "Цвет задан вручную, подходящего токена в библиотеке нет. Кандидат на новый токен или осознанное исключение.",
  },
};

/**
 * Тексты для таблицы типографики — только там, где базовые не подходят.
 * «Критичные» свойства — размер и межстрочный интервал
 * (typographyUtils.criticalTypographyMatches): от них зависит, «Совпало имя»
 * это или «Конфликт значений».
 */
/**
 * Тексты для таблицы иконок (v1.5.0, lib/iconComparator.ts). Форма важнее
 * имени; «похожесть» — доля совпадения силуэтов.
 */
const ICON_OVERRIDES: Partial<Record<StatusFilterKey, Pick<StatusMeta, "label" | "hint">>> = {
  exact: {
    label: "Совпадает с библиотекой",
    hint: "Это экземпляр иконки из библиотеки. Строка видна, только если с ним что-то не так — например, он растянут.",
  },
  value: {
    label: "Совпала форма",
    hint: "Это экземпляр другого компонента, но по форме он совпадает с иконкой библиотеки — её и стоит поставить.",
  },
  detached: {
    label: "Отвязанная иконка",
    hint: "Точная копия иконки библиотеки, но не экземпляр компонента: связь с библиотекой потеряна. Верните привязку.",
  },
  approximate: {
    label: "Похожая форма",
    hint: "Точного совпадения нет, но в библиотеке есть похожая иконка. Рядом — насколько похожа форма.",
  },
  conflict: {
    label: "Конфликт",
    hint: "Имя совпало с иконкой библиотеки, а форма заметно другая. Проверьте, та ли это иконка.",
  },
  "layout-only": {
    label: "Нет в библиотеке",
    hint: "Это экземпляр компонента, но похожей иконки в библиотеке нет ни по форме, ни по имени.",
  },
  "hardcoded-no-analog": {
    label: "Иконка без компонента",
    hint: "Иконка нарисована векторами, а не взята из библиотеки, и похожей в библиотеке нет. Кандидат на новую иконку или осознанное исключение.",
  },
  mapped: {
    label: "Решение принято",
    hint: "По этой иконке выбрана иконка из библиотеки.",
  },
};

const TYPOGRAPHY_OVERRIDES: Partial<Record<StatusFilterKey, Pick<StatusMeta, "label" | "hint">>> = {
  exact: {
    label: "Совпадает с библиотекой",
    hint: "К слою привязан стиль библиотеки, и все свойства текста с ним совпадают — делать ничего не нужно.",
  },
  value: {
    label: "Совпало значение",
    hint: "Все свойства текста совпадают со стилем библиотеки, но сам стиль к слою не привязан.",
  },
  "name-match": {
    label: "Совпало имя",
    hint: "Стиль совпал с библиотечным по имени, размер и межстрочный интервал те же, но отличаются другие свойства — шрифт, начертание или трекинг. Часто это локальная правка поверх стиля. Что именно отличается — в колонке «Что расходится».",
  },
  conflict: {
    label: "Конфликт значений",
    hint: "Стиль совпал с библиотечным по имени, но отличается размер или межстрочный интервал. Обычно в макете осталось устаревшее значение или стиль правили вручную.",
  },
  "layout-only": {
    label: "Нет в библиотеке",
    hint: "Похожего стиля текста в библиотеке не нашлось ни по имени, ни по значениям.",
  },
  "ghost-binding": {
    label: "Потерянный стиль",
    hint: "Слой ссылается на стиль текста, которого больше нет в файле. Текст выглядит как раньше, но привязка потеряна.",
  },
  "hardcoded-no-analog": {
    label: "Текст без стиля",
    hint: "Типографика задана вручную, без стиля текста, и похожего стиля в библиотеке нет. Кандидат на новый стиль или осознанное исключение.",
  },
};

export function getStatusMeta(key: StatusFilterKey, category: TokenCategory): StatusMeta {
  const base = STATUS_META[key];
  const override =
    category === "typography" ? TYPOGRAPHY_OVERRIDES[key] : category === "icons" ? ICON_OVERRIDES[key] : undefined;
  return override ? { ...base, ...override } : base;
}
