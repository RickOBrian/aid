# Аудит Token Comparator — 2026-09-17

Трекер работ по результатам аудита кода плагина. **Это точка входа для новой
сессии:** прочитать этот файл, взять нужный батч, сделать, обновить статус в
том же коммите.

## Текущее состояние на 2026-09-18

> **Номера версий.** 2026-09-18 плагин перенумерован по SemVer:
> `v0.1.0` → `v1.0.0`, `v0.1.1` → `v1.0.1`, `v0.1.2` → `v1.1.0`. Ниже
> старые номера оставлены там, где описаны события до перенумерации.
> Подробности — `token-comparator-release-guide.md`, раздел 11.

**Аудит закрыт.** Все находки разобраны, всё смержено в `main` = `bf9fcae`,
проверка в настоящей Figma пройдена по трём приоритетным сценариям (находки
№2, №3, №4 + №21) — расхождений не выявлено.

| | |
|---|---|
| Находок всего | 22 (19 из аудита + №20, №21, №22 найдены по ходу) |
| Починено | 21 |
| Отложено с условием пересмотра | №14 и №19в — обнулили бы всю историю решений |
| Открыто как улучшение | нет |

**Не у людей.** Правки после v0.1.2 (PR #19, #20, #21) в опубликованный архив
не входят: защита от дубликата PR, переименование `styleId` → `nodeId`,
подсказки в настройках и чтение реестра без ключа. Серверные части уже
работают с `main`, клиентские дойдут со следующим релизом. **Изменён
манифест** (добавлен `raw.githubusercontent.com`) — значит переимпорт
обязателен, подменой файлов не обойтись.

Перед релизом v0.1.3 стоит проверить чтение реестра в живой Figma: открыть
плагин без ключа и убедиться, что реестр подхватился. Сеть в песочнице Figma
ведёт себя не как `curl`, а манифест менялся.

**В проде.** Релиз **v0.1.2** опубликован и выдаётся кнопкой в Presentbook
(ссылка ведёт на `latest`). Бэкенд задеплоен с `main` и проверен: неверный
ключ — 401, рабочий — 200. Ключ роздан команде, плагин переимпортирован.

**Что осталось незакрытым — один пункт, осознанно.**

- **№14 и №19в** — схема идентификаторов. Условие пересмотра: ~10 000 записей
  в реестре либо реальный случай склейки одноимённых стилей.

---

## v1.2.0 — что входит

Список задаёт Principal Designer; релиз не собирается, пока здесь есть `todo`.

**Выпущено 2026-09-18.** Проверка в Figma — Principal Designer: превью
типографики, «Применить в макет» для текста (№23), чтение реестра без
ключа (№22). Scope закрыт.
Новые пункты дописываются сюда, а не держатся в голове между сессиями.

| Пункт | Статус |
|---|---|
| Превью «Было / Будет» для типографики — паритет с цветами | **fixed**, проверено в Figma |
| №23 — привязка стиля текста в `dynamic-page` | **fixed**, проверено в Figma |
| Фон превью по контрасту — белый текст не виден на светлой шахматке | **fixed** |
| Вкладка «Гайд» — актуальность, покрытие сценариев, язык | **fixed** |
| №24 — тексты статусов типографики говорили о цвете | **fixed** |
| Чейнджлог во вкладке «Гайд» | **fixed** |
| Перенумерация версий по SemVer (прошлые релизы и теги) | **fixed** |
| Проверка чтения реестра без ключа в живой Figma (№22) | **done** |
| Описание сетевых доступов в манифесте — чтение через бэкенд убрано в №22 | **fixed** |

**Превью типографики, 2026-09-18.** Кнопка «Показать превью» в строке
(статусы `value`, `name-match`, `conflict`, `name-mismatch` при найденном
стиле), под combobox «Выбрать стиль из AID» (с явным `styleId`) и в модалке
«Применить в макет». «Будет» строится не пересборкой шрифта из значений, а
привязкой того же стиля библиотеки к клону — той же операцией, что и
применение: так превью показывает ровно его результат, и не повторяется
потеря начертания из находки №6.

- Правило доступности кнопки вынесено в `src/lib/previewEligibility.ts` и
  покрыто тестами — одно на обе категории, как `statusKeys.ts`.
- Жизненный цикл клона (поиск контейнера, клон, гарантированное удаление)
  вынесен в `withPreviewClone` и общий для цветов и типографики. Поведение
  цветов не менялось — проверено в браузере.
- Заголовок модалки зависит от категории: раньше был зашит «цвет».

Тесты: `test/previewEligibility.test.ts`. Проверено в браузере на стенде с
подменой `code.ts`: кнопка ровно у четырёх статусов, сообщения
`build-preview` с `styleId` / без, превью в модалке применения, цвета без
изменений.

**Фон превью по контрасту, 2026-09-18.** Найдено Principal Designer в Figma:
белый текст без подложки экспортируется прозрачным PNG и на светлой шахматке
не виден. Фон выбирается по самому снимку — `src/lib/previewBackdrop.ts`:
если большинство непрозрачных пикселей не набирает 3:1 с клетками светлой
шахматки, а на тёмной читается лучше, снимок получает тёмную. Почти
непрозрачный снимок остаётся на светлой — шахматки под ним не видно.
Работает для обеих категорий, для каждого снимка отдельно. Новые токены —
`--ds-surface-checker-base`, `--ds-surface-checker-inverse[-base]`, core
`--core-neutral-x-85`; реестр обновлён.

Тесты: `test/previewBackdrop.test.ts`. Проверено в браузере: прозрачный PNG
с белым текстом — тёмная шахматка, с тёмным — светлая, в обеих модалках.

**Вкладка «Гайд», 2026-09-18.** Сверена с кодом. Было неверно: «Правка
значения» для цветов якобы требует найденного токена (на деле доступна при
загруженной библиотеке, токен выбирается в списке); отчёт описан только для
цветов; статусы «Совпало значение» и «Конфликт значений» объяснялись через
цвет; не было «Нет в библиотеке» и пометок «Переопределён» / «Применено
частично». Не было трёх сценариев: «Применить в макет», отправка решений на
согласование, работа со строкой (переход к слою, превью, фильтр). Гайд
переписан: восемь разделов, добавлен «Если что-то не работает», внутренние
термины убраны. В GUIDE.md попутно исправлены две устаревшие фразы — чтение
реестра через `api.github.com` (после №22 — `raw`) и «после применения
решение стирается из истории» (после батча 2 — нет).

Не обещаем в гайде: согласованные решения обратно в таблицу **не
подтягиваются** — реестр плагин только показывает на вкладке «Настройки».
Если это нужно, это отдельная фича.

**Чейнджлог во вкладке «Гайд», 2026-09-18.** Блок «Что нового» в конце
гайда: по версии на строку, от новой к старой, с датой и группами «Новое /
Изменено / Исправлено», отдельно — что сделать после обновления. Установленная
версия помечена «Ваша версия» и раскрыта, невыпущенная — «Готовится». Данные
— `src/lib/changelog.ts`, текст для дизайнеров, без номеров находок.

**Перенумерация, 2026-09-18.** Следующий релиз должен был выйти как
`0.1.3`, хотя добавляет возможности — это MINOR. Ошибка началась раньше:
`0.1.2` с типографикой тоже вышел как PATCH. По решению Principal Designer
перенумерованы все релизы, включая выпущенные, и удалены старые теги —
см. `token-comparator-release-guide.md`, раздел 11. Правила для плагина
дописаны в `semver-guide.md` (раздел «Инструменты»). `package.json` —
`1.1.0` (последняя выпущенная), следующий релиз — `1.2.0`.

**Перед релизом:** поднять версию в `package.json` и проставить дату в записи
чейнджлога. `test/changelog.test.ts` не пропустит версию без записи или без
даты — релиз без обновлённого чейнджлога не пройдёт ворота.

**Находка №24, найдена 2026-09-18** при сверке гайда. Строка типографики,
заданная вручную без аналога, получала бейдж «Цвет вручную», а подсказки
«Совпало значение», «Конфликт значений», «Потерянный стиль» объясняли
расхождение цвета. Тексты статусов вынесены из `ui.ts` в
`src/lib/statusMeta.ts`, у типографики свои там, где базовые говорят о цвете;
тональность общая. Тесты: `test/statusMeta.test.ts`, мутация роняет 5 тестов.

**Ограничение проверки.** Сторона `code.ts` (импорт стиля, клон, экспорт PNG)
живёт в Figma-рантайме и юнит-тестами не покрыта. Проверить в Figma:
строка с `conflict` — превью показывает разницу; combobox — превью выбранного
стиля; стиль со шрифтом, которого нет в системе, — внятная ошибка, а не
зависание; после закрытия превью на странице не осталось клона.

---

## Иконки — план (черновик, 2026-09-18)

Новая категория сканирования по заданию Principal Designer: иконки должны
быть экземплярами компонентов из библиотеки; если иконка не оттуда —
подобрать библиотечную по форме (приоритетнее) или по имени. Реализация
не начата — сначала план и ответы на открытые вопросы.

**Что известно.** Каталог иконок Driver — отдельный файл Figma, имена
`категория/имя` (`action/camera`), размер по умолчанию 24×24, векторный слой
внутри — «🎨 Color»; 443 экспортированных SVG лежат в
`pages/driver-color-tokens/public/icons`. REST: `/v1/files/:key/components`
и `/component_sets` — опубликованные компоненты, `/nodes?ids=…&geometry=paths`
— их геометрия. Plugin API: `getMainComponentAsync`, `fillGeometry` /
`strokeGeometry`, `absoluteTransform`. Путь Figma — только команды
M, L, Q, C, Z.

### Как сравнивать форму

Отпечаток формы — растр 32×32 из геометрии иконки (заливка и обводка,
нормализованные в квадрат с сохранением пропорций). Похожесть — доля
совпадения масок (IoU). Растеризация — своя, чистым кодом (разбор пути,
аппроксимация кривых, заливка строками по правилу nonzero/evenodd): так она
одинаково работает в песочнице плагина, где нет canvas, и в тестах.
Отпечатки библиотеки считаются при загрузке и хранятся в данных библиотеки
(~200 байт на иконку). Цвет на форму не влияет.

Пороги (уточнить на шаге 0): ≥ 0.90 — «Совпала форма», 0.75–0.90 —
«Похожая форма» (аналог «Близкое значение» у цветов).

### Статусы

| Случай | Статус |
|---|---|
| Экземпляр компонента этой библиотеки | скрыт (как `exact`) |
| Экземпляр чужого или локального компонента, форма совпала | «Совпала форма» |
| Не компонент (вектор, группа векторов), форма совпала | «Иконка без компонента» + предложение |
| Имя совпало, форма заметно другая | «Конфликт» |
| Форма похожа, не совпала | «Похожая форма» |
| Ничего похожего | «Нет в библиотеке» |

При расхождении формы и имени предлагается иконка по форме, имя —
в подсказке.

### Этапы

0. **Проверка подхода на реальных иконках** — до кода плагина. На 443 SVG
   из Presentbook: насколько отпечатки различают иконки, какие пары
   библиотеки неразличимы, устойчивость к перекраске, масштабу, обводке,
   переведённой в контур. Итог — пороги и решение «идём / меняем подход».
1. Библиотека: компоненты, геометрия, отпечатки, кэш в данных библиотеки.
2. Сканер: экземпляры и «похожие на иконку» векторные узлы, группировка —
   по ключу компонента или по отпечатку.
3. Компаратор, статусы, тексты статусов для иконок (`statusMeta`).
4. UI: категория «Иконки», превью иконок в таблице (макет — экспорт PNG,
   библиотека — SVG из геометрии, без сети), выбор иконки из списка.
5. Решения, экспорт, печать, реестр (`targetComponentKey`,
   `targetComponentName`, категория `icons`; бэкенд — MINOR).
6. «Применить в макет» и превью: экземпляр — `swapComponent`, вектор —
   замена экземпляром на том же месте и в том же размере.

### Идеи сверх задания

- **Нестандартный размер** — иконка растянута не по сетке библиотеки.
- **Отвязанная иконка** — точная копия библиотечной, но не экземпляр:
  восстановить привязку в один клик.
- **Устаревшие иконки** — пометка deprecated в описании компонента →
  статус и замена.
- **Дубликаты в библиотеке** — неразличимые по форме компоненты: отчёт для
  владельца дизайн-системы.
- **Варианты** (размер, outline / solid в наборах компонентов) —
  предлагать подходящий вариант, а не первый.

### Открытые вопросы

1. Как отличить иконки от прочих компонентов библиотеки: иконочная
   библиотека — отдельный файл (выбирается в сканировании), или фильтр по
   странице / фрейму / имени?
2. «Применить в макет» для вектора без компонента заменяет его экземпляром:
   локальные правки вектора теряются. Годится?
3. Версия: отдельный релиз после v1.4.0?
4. Какие идеи сверх задания брать.

---

## v1.4.0 — что входит

Scope задан Principal Designer 2026-09-18: из бэклога — только жизненный цикл
решений; плюс новый функционал, который он опишет. Релиз не собирается, пока
здесь есть `todo`.

| Пункт | Статус |
|---|---|
| Жизненный цикл отправленных решений — «На согласовании», «Отклонено» | todo |
| Несколько библиотек: загрузка списка в настройках, выбор одной в сканировании | **fixed**, ждёт проверки в Figma |
| №25 — решения реестра по типографике читались без стиля | **fixed** |
| Пустая таблица до сканирования писала «Расхождений нет» | **fixed** |
| Бэкенд `1.1.0` — поле библиотеки в записи реестра | **fixed** |

**Несколько библиотек, 2026-09-18.** Схема — Principal Designer: в
настройках загружается список, в сканировании выбирается одна. Поэтому
приоритета между библиотеками нет — сравнение всегда с одной.

- Хранение: список `tc_libraries` отдельно от данных, данные каждой — под
  своим ключом `tc_library_data:<fileKey>` (лимит `clientStorage` ~5 МБ на
  все ключи, одна запись с несколькими кэшами упёрлась бы в него раньше).
  Остальной код работает с «текущей» библиотекой через `lastLibraryColors` /
  `lastLibraryTypography` — сравнение, превью и применение не менялись.
- Идентификаторы переменных и стилей уникальны только внутри файла. Решения
  с токеном хранят `libraryFileKey` и действуют только в своей библиотеке
  (`lib/libraryScope.ts`); `ignored` / `candidate` — везде. Миграция
  привязывает старые решения к прежней единственной библиотеке. В реестр
  уходит `targetLibraryFileKey`, в описание запроса — название библиотеки.
- Смена библиотеки сбрасывает результаты сканирования.
- **Компактный вид — по замечанию Principal Designer.** Кнопки-иконки
  вместо текстовых (новый общий примитив `.ds-icon-btn`), в списке только
  название файла библиотеки, подпись — лишь если что-то не загрузилось.
  Принцип касается всего интерфейса плагина: где можно, текстовую кнопку
  заменять иконкой, лишние подписи не выводить.

Тесты: `test/libraries.test.ts`. Проверено в браузере: добавление, статус,
выбор, недоступная типографика у библиотеки без стилей, удаление, пустое
состояние. Проверить в Figma: миграция существующей библиотеки (решения на
месте), загрузка второй, сканирование с каждой, удаление.

**Находка №25, найдена 2026-09-18** при добавлении поля библиотеки.
`parseRegistryEntry` в плагине не читал `category`, `targetStyleId`,
`targetStyleName`, `mismatchedProperties` — согласованные решения по
типографике из v1.3.0 подтягивались без стиля и строку не закрывали.
Тест в `test/registryDecisions.test.ts`, мутацией проверено.

**Пустая таблица.** До первого сканирования и после смены библиотеки таблица
писала «Расхождений нет: цвета уже привязаны…», хотя сканирования не было.
Теперь — «Сканирования ещё не было».

**Жизненный цикл — черновой план.** Остаток P0.1 из
`github-sync-architecture.md`: согласованные решения плагин уже узнаёт
(v1.3.0), но не знает, что отправленное ждёт согласования или отклонено.

- Статусы — через бэкенд, не напрямую с GitHub: у неавторизованных запросов
  к `api.github.com` лимит 60 в час на IP (урок №22). Новый эндпоинт
  принимает подписи и возвращает по каждой: `open` (номер и ссылка на
  запрос) или `rejected` (запрос закрыт без мержа). `approved` уже видно по
  реестру `main`.
- На ветке `registry/propose-*` лежит весь реестр, а не только новые
  решения, поэтому «что предложено в этом запросе» — разница с `main`. Та же
  логика уже есть в защите от дубликата (`listOpenProposalBranches`,
  `fetchRegistryFileOnBranch`), её надо обобщить на закрытые запросы.
- В плагине: пометка «На согласовании» со ссылкой; при отклонении подпись
  выходит из `tc_submitted_signatures`, строка снова в очереди на отправку с
  пометкой «Отклонено».
- Открытые вопросы — задать перед реализацией: требовать ли ключ для
  чтения статусов (бэкенд тратит квоту своего токена); показывать ли
  комментарий, с которым запрос закрыли.

---

## v1.3.0 — что входит

**Выпущено 2026-09-18.** Проверка в Figma — Principal Designer.

| Пункт | Статус |
|---|---|
| Согласованные решения из реестра — в таблице у всех | **fixed**, проверено в Figma |
| Бэкенд `aid-registry-api` — первая формальная версия `1.0.0` | **fixed** |
| Ветка `cursor/figma-token-comparator-github-sync` | **закрыта** |

**Решения из реестра, 2026-09-18.** Реестр в `main` хранит только одобренные
решения, но плагин его только показывал: согласованное одним дизайнером у
остальных не появлялось. Теперь перед сравнением решения реестра
подмешиваются к локальной истории (`src/lib/registryDecisions.ts`).
Решения по умолчанию, принятые без отдельного обсуждения:

- **локальное решение важнее реестра** — если дизайнер решил иначе, он,
  скорее всего, хочет предложить замену;
- решения реестра **не попадают в очередь на согласование** и не пишутся в
  `clientStorage`: реестр перечитывается при запуске, копия устаревала бы;
- строки, закрытые решением реестра, скрываются как свои, но статус
  сканирования называет их количество; остальные получают бейдж
  «Согласовано».

Не сделано: жизненный цикл предложений (открыт / отклонён) — отдельный пункт
P0 в `github-sync-architecture.md`. Тесты: `test/registryDecisions.test.ts`.
Проверить в Figma: скан файла, где есть группы с решениями из реестра, —
строки скрыты и посчитаны, «Согласовано» на остальных, счётчик «Отправить N
решений» решения реестра не включает.

**Бэкенд.** `server/package.json` → `1.0.0`: первая формальная версия по
`semver-guide.md`. Потребитель бэкенда — плагин, поэтому MAJOR для него —
несовместимое изменение API. Номер в рантайме не используется.

**Ветка `cursor/figma-token-comparator-github-sync`.** Разобрана по
содержимому: 734 из 797 файлов идентичны `main`, 22 — старые версии, которые
`main` перерос (такая же версия была в его истории), 3 — тоже старые, но
без точного следа (`.gitignore`, `package.json` и lock презентбука: в
`main` у них больше строк, а не меньше), уникален только `zoom1.png` —
WIP-скриншот. Архивный тег `archived/cursor/figma-token-comparator-github-sync`
уже стоял с 2026-09-17 на её вершине; ветка удалена.

---

## Как работаем

1. **Один батч — одна сессия — один коммит.** За пределы своего батча не
   выходим: замеченное попутно записываем сюда в «Замечено попутно», а не
   чиним на месте.
2. **Сначала тест, который падает,** потом правка, потом тест зелёный. Для
   известных дефектов уже стоят заглушки `it.todo` с номером находки —
   батч начинается с превращения `todo` в настоящий тест.
3. **Ворота перед коммитом:** `npm run typecheck`, `npm run build`,
   `npm test` в плагине, `npm test` в `server/`. Для UI-батчей — ещё прогон
   в браузере.

```bash
cd tools/figma-token-comparator
npm run typecheck && npm run build && npm test
cd server && npm test
```

## Тестовая обвязка

- `vitest.config.mts` — тесты берутся из `test/**/*.test.ts`
- `test/figmaStub.ts` — заглушка глобального `figma`: символ `figma.mixed` и
  `clientStorage` в памяти; подключается как setup-файл, сбрасывается через
  `resetFigmaStub()`
- `test/fixtures.ts` — конструкторы записей макета и токенов библиотеки
- Покрыты чистые модули: оба компаратора, нормализация и применение
  типографики, история решений, экспорт. `scanner` и обход документа
  юнит-тестами не покрыты — им нужен настоящий Figma-рантайм, они
  проверяются вручную.

## Статусы находок

Легенда: `todo` — не начато, `wip` — в работе, `fixed` — починено,
`declined` — решили не чинить (с обоснованием).

### Батч 1 — безопасность

| № | Находка | Где | Статус |
|---|---|---|---|
| 1 | Общий секрет бэкенда инлайнится в `dist/code.js` и уезжает в публичный релиз | `src/lib/storage.ts`, `build.js` | **fixed** |

Сделано 2026-09-18, два этапа.

Первый — аварийный: ключ отозван и заменён, бэкенд передеплоен, опубликован
релиз `v0.1.1`, ассеты `v0.1.0` удалены. Это закрыло утёкший ключ, но не
причину: новый точно так же лежал в новом архиве.

Второй — по существу: **ключ вынесен из сборки в настройки плагина**, туда же,
где уже живут токен Figma и токен GitHub. `build.js` больше не подставляет
`PLUGIN_SHARED_SECRET`, `pluginSecrets.d.ts` удалён, `release.mjs` не требует
секрета для упаковки. Клиенты бэкенда получают ключ параметром из
`storage.getRegistrySecret()`. Проверено: в свежей сборке значения из
`.env.local` нет.

Что это меняет и чего не меняет: ключ больше не читается любым, кто скачал
архив, — его знают только те, кому его выдали. Но это по-прежнему **общий**
секрет: отзыв означает смену у всех, и любой, кому его дали, может слать
запросы от чужого имени. Настоящее решение — авторизация каждого дизайнера
своей учётной записью GitHub (вариант В); оно требует OAuth-приложения и
остаётся целью, а не сделано.

Без ключа плагин работает: сканирование, сравнение, решения и экспорт
доступны. Недоступен только реестр — статус на вкладке «Настройки» и отправка
на согласование.

### Батч 2 — потеря решений

| № | Находка | Где | Статус |
|---|---|---|---|
| 2 | Отправленную подпись никогда не снимают — изменённое решение нельзя отправить повторно | `src/lib/storage.ts` | **fixed** |
| 3 | Частичное применение цвета стирает решение целиком (`applied > 0`) | `src/lib/decisionPersistence.ts` | **fixed** |
| 7 | Решение пишется в историю до проверки, что строка существует | `src/code.ts` | **fixed** |
| 18 | Решение с исчезнувшим токеном скрывает строку молча | оба компаратора | **fixed** |

Сделано 2026-09-18:

- `clearSubmittedSignature` снимает отметку «отправлено» при изменении и
  отмене решения. Служебные перезаписи (учёт применённых слоёв) передают
  `{ keepSubmitted: true }`, иначе уже отправленное решение уехало бы в
  реестр повторно.
- Логика «что делать с решением после применения в макет» вынесена из
  `code.ts` в `src/lib/decisionPersistence.ts` — общая для цветов и
  типографики. Раньше ветки расходились, из-за чего и возникла находка №3.
  Поля учёта применения переписываются целиком: успешный повтор снимает
  `applyPartial` и старые пропуски.
- `handleApplyDecision` проверяет наличие строки до записи в историю.
- `requiresUserAction` (и типографический аналог) закрывают строку не по
  факту решения, а по существованию его цели: статус `mapped` ставится
  только когда токен найден. Находка №18 в типографике была симметричной и
  закрыта тем же изменением.
- Попутно закрыта часть находки №19: `applyHistory` компаратора цветов
  переносит `applyPartial` / `applySkips`, цветовая строка рисует бейдж
  «Применено частично». Остаток №19 (перенос `decisionTargetStyleId`, режим
  цели, ключ группировки) остаётся в батче 4.

Тесты: `test/storage.test.ts`, `test/decisionPersistence.test.ts`,
`test/colorComparator.test.ts`, `test/typographyComparator.test.ts`.
Проверено мутацией: возврат старого поведения роняет 5 тестов.

### Батч 3 — типографика

| № | Находка | Где | Статус |
|---|---|---|---|
| 4 | Переопределения библиотечных стилей отфильтровываются, бейдж «Переопределён» недостижим | `src/comparators/typographyComparator.ts` | **fixed** |
| 5 | Вес шрифта угадывается по имени начертания; флаг `fontWeightApproximate` при сравнении не учитывается | `src/lib/typographyUtils.ts` | **fixed** |
| 6 | Обратное преобразование веса в начертание теряет курсив и нестандартные имена | `src/lib/typographyApply.ts` | **fixed** |
| 10 | `typographyUnresolved: typographyUnresolved \|\| true` — константное выражение | `src/lib/scanner.ts` | **fixed** |

Сделано 2026-09-18:

- **№4.** Решение по открытому вопросу — переопределения **показываем**. Сама
  привязка библиотечного стиля больше не закрывает строку: `findRecordStatus`
  сверяет значения слоя со значениями стиля и при расхождении выдаёт
  `name-match` / `conflict` с перечнем разошедшихся свойств, а
  `requiresTypographyUserAction` закрывает строку только при статусе `exact`.
  Пользователь видит в колонке «Что расходится» ровно переопределённые
  свойства.
- **№5.** `inferFontWeightFromFontName` переписан таблицей «от специфичного к
  общему» — «Demi Bold» больше не попадает под проверку на «bold».
  `typographyValuesEqual` исключает вес из сравнения, если хотя бы одна
  сторона помечена `fontWeightApproximate`. **`typographyValueKey` при этом не
  тронут** — он определяет идентификаторы записей, к которым привязаны
  сохранённые решения.
- **№6.** `resolveFontNameForApply` берёт начертание с самого слоя, когда
  семейство не меняется (основной сценарий), и собирает его из веса только
  при смене семейства, перенося наклон. `fontNameFromTypographyValue` удалён
  за ненадобностью.
- **№10.** Константное выражение убрано.

Тесты: `test/typographyComparator.test.ts`, `test/typographyUtils.test.ts`.
Проверено мутацией: возврат старого поведения роняет 2 теста.
(`test/typographyApply.test.ts` удалён вместе с находкой №20 — проверял
функцию, у которой не осталось вызывающих.)

**Побочный эффект №4, за которым стоит последить.** Переопределения —
частая штука в реальных файлах, и теперь они попадают в таблицу. Если
типографика окажется зашумлённой, лечится это фильтром по статусу из
находки №15 (батч 6), а не возвратом старого поведения: данные теперь верные.

### Батч 4 — точность данных

| № | Находка | Где | Статус |
|---|---|---|---|
| 11 | В реестр уходит подпись комбобокса `имя (коллекция)` вместо имени токена | `src/lib/libraryLabels.ts`, `src/ui.ts` | **fixed** |
| 12 | «Применено к N из M слоёв»: M считается как `applied + skipped`, а не размер группы | `src/ui.ts` | **fixed** |
| 13 | `count` группы может превышать `nodeIds` (лимит 500) — «полное» применение неполно | `src/lib/scanner.ts`, `src/ui.ts` | **fixed** |
| 19 | Мелочи компаратора цветов | `src/comparators/colorComparator.ts` | **fixed частично** |

Сделано 2026-09-18:

- **№11.** Пара «форматирование подписи ↔ поиск по подписи» вынесена в
  `src/lib/libraryLabels.ts` и покрыта тестами: держать их врозь и было
  причиной находки. В реестр теперь уходит имя выбранного объекта, а не
  подпись из списка. То же исправлено для стилей текста — там подпись
  сейчас совпадает с именем, но полагаться на это было нельзя.
- **№12.** Сообщение о результате получает реальный размер группы:
  `attempted` (сколько слоёв пытались изменить) и `occurrences` (сколько
  вхождений в группе) приходят из `code.ts` в сообщении
  `apply-to-layout-result`. Знаменатель больше не выводится из числителя.
- **№13.** Сканер помечает группу `nodeIdsTruncated`, если список слоёв
  обрезан лимитом. Подтверждающая модалка показывает «500 из 700» вместо
  «700», а после применения выводится предупреждение, что за один заход
  обрабатывается не больше сохранённого списка.
- **№19.** Перенесён `decisionTargetStyleId`; подтверждённый маппинг
  показывает режим, значение которого реально совпало, а не нулевой.

**Не сделано осознанно** — третий подпункт №19, ключ группировки без
`variableId` / `styleId`. Ключ определяет `LayoutRecord.id`, то есть
идентификатор, к которому привязаны все сохранённые решения и подписи в
реестре. Его изменение обнулит историю ровно так же, как замена схемы
идентификаторов из находки №14, поэтому оно туда и переносится: делать их
надо одной миграцией, а не по отдельности.

Тесты: `test/libraryLabels.test.ts`, `test/colorComparator.test.ts`.
Проверено мутацией: возврат старого поведения роняет 2 теста.

**Ограничение проверки.** Правки №11 (call-site), №12 и №13 живут в
`src/ui.ts`, который юнит-тестами не покрыт: мутация `targetName` тесты не
роняет. Они держатся на вынесенном и покрытом помощнике, на типах и на
чтении кода — но не на тесте. Это первые кандидаты на проверку в Figma.

### Батч 5 — бэкенд

| № | Находка | Где | Статус |
|---|---|---|---|
| 9 | Реестр пополняется дубликатами подписей; нет идемпотентности и защиты от гонки версий | `server/api/_lib/mergeRegistryEntries.ts` | **fixed** |

Сделано 2026-09-18. Решение по открытому вопросу: **новое решение заменяет
старое**. Реестр хранит текущее решение по каждой подписи, история правок
остаётся в git — она видна в диффе pull request'а.

- Слияние вынесено в `mergeRegistryEntries.ts` и покрыто тестами: замена по
  подписи с сохранением места записи в файле (чтобы дифф читался как правка
  одной строки, а не удаление из середины), схлопывание дубликатов внутри
  одного запроса и дубликатов, накопившихся в реестре раньше.
- Повторная отправка уже записанного решения не заводит pull request:
  сравнение идёт по содержанию решения, без `proposedAt` / `proposedBy`.
  Бэкенд отвечает `{ success: true, unchanged: true }`, плагин показывает
  «Эти решения уже записаны в реестре» вместо «ждёт согласования».

Дозакрыто 2026-09-18: перед созданием pull request бэкенд смотрит открытые
ветки `registry/propose-*` и читает реестр на каждой. Если ровно эти решения
там уже лежат — новый PR не заводится, ответ приходит с
`reason: "already_proposed"`, и плагин пишет «уже отправлены и ждут
согласования» вместо «отправлено».

Решение с **другим** содержанием по той же подписи дубликатом не считается —
оно уезжает новым PR, иначе изменение потерялось бы. Сбой самой проверки не
блокирует отправку: если GitHub не ответил, PR создаётся. Лишний pull request
— меньшее зло, чем потерянное решение.

**Счётчик `registryVersion` остаётся неточным.** Два предложения, созданные
от одного состояния `main`, получают одинаковый номер; git сольёт обе правки
без конфликта, и номер окажется занижен. Ни одна ветка логики на нём не
завязана — он только показывается в статусной строке плагина, поэтому
оставлено как есть с комментарием в коде.

**Изменение не в проде.** Бэкенд деплоится из `main`, а правка лежит в ветке
`feat/token-comparator-typography` — она заработает после мержа.

### Батч 6 — интерфейс

| № | Находка | Где | Статус |
|---|---|---|---|
| 8 | Кнопка отправки включается по общему счётчику, а выключается по счётчику активной категории | `src/ui.ts` | **fixed** |
| 15 | У типографики нет фильтра по статусу, экспорта, печати и счётчика «обработано» | `src/lib/exporter.ts`, `src/ui.ts`, `src/ui.html` | **fixed** |
| 16 | Первая строка выделяется автоматически — один клик пишет решение по невыбранной строке | `src/ui.ts` | **fixed** |
| 21 | Тип привязки подменял статус в обеих категориях: все строки типографики со стилем получали бейдж «Стиль вместо токена» | `src/lib/statusKeys.ts` | **fixed** |

Сделано 2026-09-18. Решения по открытым вопросам: типографике — **полный
паритет** (фильтр, экспорт, печать, счётчик); автовыделение — **убрать**.

- **№8.** Состояние кнопки отправки считает одна функция `syncProposeButton`
  по счётчику активной категории. Раньше открытие и закрытие модалки включали
  кнопку обратно по общему счётчику, и она оказывалась активной с подписью
  «Отправить 0 решений» — клик по ней не делал ничего.
- **№15.** Набор колонок экспорта выбирается по категории: у типографики нет
  режимов библиотеки, зато есть «Что расходится». JSON проецируется на тот же
  набор, иначе выгрузка типографики тащила бы пустые колонки режимов.
  Figma-таблица получает категорию через `MappingTableOptions`. Фильтр статусов
  обобщён на обе таблицы, сводка со счётчиком «обработано» стала общей.
- **№16.** `restorePreferredSelection` восстанавливает выделение только если
  строка действительно была выбрана раньше. Ничего не выбрано — «Применить
  решение» неактивна.
- **№21.** Найдено при проверке батча в браузере. `getResultStatusFilterKey`
  подменял статус типом привязки для обеих категорий. Для цветов это верно:
  цвет через paint-стиль нельзя перепривязать автоматически. Для типографики
  стиль текста — это и есть токен, и подмена съедала весь результат сравнения:
  все строки получали один бейдж, фильтр предлагал один пункт, а расхождения,
  вычисленные в батче 3, до пользователя не доходили. Логика вынесена в
  `src/lib/statusKeys.ts` и покрыта тестами.

Тесты: `test/exporter.test.ts`, `test/statusKeys.test.ts`. Проверено в
браузере на пяти строках типографики: пять разных бейджей, пять пунктов
фильтра, CSV с колонками типографики, отсутствие автовыделения.

### Найдено в ходе работ

| № | Находка | Где | Статус |
|---|---|---|---|
| 20 | «Применить в макет» для типографики с решением «правка значения» применяет к слоям их же собственные значения | `src/code.ts`, `src/ui.ts` | **fixed** |
| 22 | Ключ реестра требуется и для чтения, хотя реестр лежит в публичном репозитории | `src/lib/githubApi.ts`, `src/code.ts` | **fixed** |
| 23 | «Применить в макет» для типографики присваивает `textStyleId`, а в `dynamic-page` это свойство только для чтения | `src/lib/typographyApply.ts` | **fixed** |
| 24 | Бейджи и подсказки статусов в таблице типографики говорят о цвете («Цвет вручную» у текста) | `src/lib/statusMeta.ts` | **fixed** |
| 25 | Решения реестра по типографике читались без категории и стиля-цели | `src/lib/githubApi.ts` | **fixed** |

Найдено 2026-09-18 в батче 3. Для решения `value_fix_proposed` кнопка
доступна, а `propertyValue` берётся как `readRecordTypographyValue(record)` —
то есть текущая типографика самой группы. Применение её же к своим слоям
рендер не меняет, но проставляет свойства явно (слой получает override
поверх стиля).

Решено 2026-09-18: **кнопку убрать**. Менять по такому решению должна
библиотека, а плагин в библиотеку писать не умеет — применять слоям их же
значения бессмысленно по определению. GUIDE.md, раздел 8, и так утверждал,
что кнопки для `value_fix_proposed` нет: код расходился с собственной
документацией, теперь сходится.

Вместе с путём удалён и режим «применить свойства без стиля» в
`typographyApply.ts` — других потребителей у него не было. Заодно ушла
`resolveFontNameForApply` из батча 3 и её тесты: функция решала проблему
курсива внутри именно этой операции.

**Находка №22, найдена 2026-09-18.** Проверено: репозиторий `RickOBrian/aid`
**публичный**, и `decisions-registry.json` читается кем угодно без всякой
авторизации:

```
raw.githubusercontent.com/RickOBrian/aid/main/decisions-registry.json → HTTP 200
```

Значит ключ на пути **чтения** не защищает ничего — он лишь мешает. Защищать
надо только отправку: там бэкенд создаёт pull request серверным токеном
GitHub.

Как должно быть:

| Что делает дизайнер | Что нужно |
|---|---|
| Сканирует, сравнивает, экспортирует, видит принятые решения | только токен Figma |
| Отправляет решения на согласование | ещё и ключ реестра |

Технически: читать реестр напрямую из репозитория, а ключ спрашивать только
для отправки.

**Решено 2026-09-18: репозиторий остаётся публичным, ключ из чтения убран.**

- `githubApi.fetchPublicRegistry` читает реестр через
  **`raw.githubusercontent.com`** без авторизации; домен добавлен в
  `networkAccess` манифеста.
- Читающий клиент бэкенда и `REGISTRY_GET_URL` удалены — у них не осталось
  вызывающих.
- Ключ спрашивается только для отправки. Поле переименовано в «Ключ для
  отправки решений», при попытке отправить без него приходит внятное
  сообщение вместо общего «не удалось».
- **Почему не `api.github.com`.** Первая версия правки читала через него, и
  это было ошибкой: у неавторизованных запросов там лимит 60 в час **на IP**,
  общий для всех за одним NAT. Поймано сразу после мержа при проверке в
  проде — `HTTP 403, x-ratelimit-remaining: 0`; квота выжигается за одну
  активную сессию, а плагин показал бы «реестр недоступен» без объяснения.
  Исправлено отдельным PR (#21): `raw` раздаётся через CDN и такого лимита не
  имеет. Цена — кэш до 5 минут, для реестра это неважно.
- `sha` у raw нет, его роль играет `ETag` — непрозрачная метка версии, по
  наличию которой отличают настоящий реестр от локального пустого.

Побочный эффект, за которым стоит следить: путь чтения теперь завязан на
публичность репозитория. Если `aid` когда-нибудь закроют, чтение сломается и
придётся возвращать бэкенд — это записано здесь, чтобы связь не потерялась.

**Находка №23, найдена 2026-09-18** при работе над превью типографики. В
манифесте `documentAccess: "dynamic-page"`, а в этом режиме
`TextNode.textStyleId` только для чтения: по документации Figma стиль
привязывается через `setTextStyleIdAsync`. `applyImportedTextStyleToNode`
присваивал свойство напрямую. Типы Figma сеттер не запрещают, поэтому
typecheck молчал, а юнит-тестов на модуль не было. В живой Figma
«Применить в макет» для типографики должен был пропускать каждый слой с
ошибкой — это следует из документации, но **в Figma не воспроизведено**:
проверка применения типографики в список сценариев аудита не входила.

Исправлено заменой на `setTextStyleIdAsync`. Для цветов та же ловушка уже
была обойдена — `setStrokeStyleIdAsync` в `handleApplyToLayout`.

Тест: `test/typographyApply.test.ts` — нода, у которой сеттер `textStyleId`
бросает, как в `dynamic-page`. Проверено: до правки тест падал.

### Батч 7 — идентификаторы (отложено)

| № | Находка | Где | Статус |
|---|---|---|---|
| 14 | 32-битный `stableHash` служит вечным идентификатором решения и подписью в реестре | `src/lib/colorUtils.ts` | отложено |
| 19в | Ключ группировки не включает `variableId` / `styleId`: одноимённые стили из разных папок склеиваются в одну строку | `src/lib/scanner.ts` | отложено |

**Решение 2026-09-18: не делать, риск принят.** Обе правки меняют
`LayoutRecord.id`, то есть обнуляют все накопленные решения и подписи в
реестре — у всех сразу. Сегодняшний вред несопоставим с ценой: при сотнях и
тысячах подписей вероятность коллизии 32-битного хеша — доли процента, а
склейка одноимённых стилей из разных папок встречается редко и видна глазом
по пути слоя.

Условие пересмотра — любое из двух:

- в реестре набралось порядка **10 000** записей (тогда коллизии становятся
  ожидаемыми, а не гипотетическими);
- кто-то реально столкнулся со склейкой двух разных стилей в одну строку.

Делать тогда одной миграцией: новая схема идентификатора с версией и
таблицей соответствия старых подписей новым, иначе история потеряется.

## Открытые продуктовые вопросы

Задаются в начале соответствующего батча, чтобы решение не забылось.

| Батч | Вопрос | Решение |
|---|---|---|
| 1 | Модель доступа к бэкенду | **ключ вынесен в настройки**; В остаётся целью (2026-09-18) |
| 2 | Решение применили в макет: считать закрытым или всё равно отправлять в реестр? | **отправлять** (2026-09-18) |
| 3 | Переопределения библиотечных стилей: показывать в таблице или убрать детекцию? | **показывать** (2026-09-18) |
| 5 | Дубликат подписи в реестре: заменять, отклонять или разрешать обе записи? | **заменять** (2026-09-18) |
| 6 | Типографике нужны фильтр и экспорт, или честно пишем «только для цветов»? | **полный паритет** (2026-09-18) |
| 6 | Автовыделение первой строки: оставить или требовать явного клика? | **убрать** (2026-09-18) |
| — | №22: останется ли репозиторий `aid` публичным? | **да, остаётся** (2026-09-18) |
| — | №20: что должна делать «Применить в макет» для строки с правкой значения? | **убрать кнопку** (2026-09-18) |

## Замечено попутно

Сюда пишем то, что нашли вне своего батча, чтобы не отвлекаться.

- ~~Версия `0.1.1` проставлена только в архиве~~ — закрыто: `package.json`
  зафиксирован на `0.1.2` коммитом.
- ~~`styleId` библиотечного стиля — это `node_id` из REST~~ — закрыто
  2026-09-18: поле переименовано в `LibraryTextStyle.nodeId`, комментарий
  исправлен (он утверждал обратное — что значение совпадает с `textStyleId`
  в макете). Заодно убраны два сравнения по идентификаторам, которые никогда
  не срабатывали: сопоставление идёт только по `key`. Для кэша стилей,
  записанного до переименования, добавлена миграция при чтении — иначе
  плагин молча перестал бы находить целевой стиль сохранённого решения.
