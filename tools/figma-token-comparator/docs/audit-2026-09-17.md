# Аудит Token Comparator — 2026-09-17

Трекер работ по результатам аудита кода плагина. **Это точка входа для новой
сессии:** прочитать этот файл, взять нужный батч, сделать, обновить статус в
том же коммите.

## Как работаем

1. **Один батч — одна сессия — один коммит.** За пределы своего батча не
   выходим: замеченное попутно записываем сюда в «Замечено попутно», а не
   чиним на месте.
2. **Сначала тест, который падает,** потом правка, потом тест зелёный. Для
   известных дефектов уже стоят заглушки `it.todo` с номером находки —
   батч начинается с превращения `todo` в настоящий тест.
3. **Ворота перед коммитом:** `npm run typecheck`, `npm run build`,
   `npm test` в плагине, `npm test` в `server/`. Для UI-батчей — ещё прогон
   в браузере.

```bash
cd tools/figma-token-comparator
npm run typecheck && npm run build && npm test
cd server && npm test
```

## Тестовая обвязка

- `vitest.config.mts` — тесты берутся из `test/**/*.test.ts`
- `test/figmaStub.ts` — заглушка глобального `figma` (нужен только
  `figma.mixed`); подключается как setup-файл
- `test/fixtures.ts` — конструкторы записей макета и токенов библиотеки
- Покрыты чистые модули: компараторы цветов и типографики, нормализация
  типографики, экспорт. `scanner`, `storage`, apply-to-layout юнит-тестами
  не покрыты — им нужен настоящий Figma-рантайм, они проверяются вручную.

## Статусы находок

Легенда: `todo` — не начато, `wip` — в работе, `fixed` — починено,
`declined` — решили не чинить (с обоснованием).

### Батч 1 — безопасность

| № | Находка | Где | Статус |
|---|---|---|---|
| 1 | Общий секрет бэкенда инлайнится в `dist/code.js` и уезжает в публичный релиз | `src/lib/registryApiConfig.ts`, `build.js` | **частично** |

Сделано 2026-09-18: ключ отозван и заменён, бэкенд передеплоен, собран и
опубликован релиз `v0.1.1` с новым ключом, ассеты `v0.1.0` удалены.
Причина не устранена: новый ключ так же лежит в новом архиве. Требуется
выбор модели (см. «Открытые продуктовые вопросы»).

### Батч 2 — потеря решений

| № | Находка | Где | Статус |
|---|---|---|---|
| 2 | Отправленную подпись никогда не снимают — изменённое решение нельзя отправить повторно | `src/lib/storage.ts:201,213`, `src/code.ts:413` | todo |
| 3 | Частичное применение цвета стирает решение целиком (`applied > 0`) | `src/code.ts:1332` | todo |
| 7 | Решение пишется в историю до проверки, что строка существует | `src/code.ts:925` | todo |
| 18 | Решение с исчезнувшим токеном скрывает строку молча | `src/comparators/colorComparator.ts:245` | todo |

Тесты-заглушки: `test/colorComparator.test.ts` → `№18`.

### Батч 3 — типографика

| № | Находка | Где | Статус |
|---|---|---|---|
| 4 | Переопределения библиотечных стилей отфильтровываются, бейдж «Переопределён» недостижим | `src/comparators/typographyComparator.ts:56` | todo |
| 5 | Вес шрифта угадывается по имени начертания; флаг `fontWeightApproximate` при сравнении не учитывается | `src/lib/typographyUtils.ts:249` | todo |
| 6 | Обратное преобразование веса в начертание теряет курсив и нестандартные имена | `src/lib/typographyApply.ts:8` | todo |
| 10 | `typographyUnresolved: typographyUnresolved \|\| true` — константное выражение | `src/lib/scanner.ts:557` | todo |

Тесты-заглушки: `test/typographyComparator.test.ts` → `№4`;
`test/typographyUtils.test.ts` → `№5`, `№6`.

### Батч 4 — точность данных

| № | Находка | Где | Статус |
|---|---|---|---|
| 11 | В реестр уходит подпись комбобокса `имя (коллекция)` вместо имени токена | `src/ui.ts:2632` | todo |
| 12 | «Применено к N из M слоёв»: M считается как `applied + skipped`, а не размер группы | `src/ui.ts:1450` | todo |
| 13 | `count` группы может превышать `nodeIds` (лимит 500) — «полное» применение неполно | `src/lib/scanner.ts:43` | todo |
| 19 | Мелочи компаратора цветов: не переносится `decisionTargetStyleId`, режим цели всегда нулевой, ключ группы без `variableId`/`styleId` | `src/comparators/colorComparator.ts`, `src/lib/scanner.ts:322` | todo |

Тесты-заглушки: `test/colorComparator.test.ts` → `№19`.

### Батч 5 — бэкенд

| № | Находка | Где | Статус |
|---|---|---|---|
| 9 | Реестр пополняется дубликатами подписей; нет идемпотентности и защиты от гонки версий | `server/api/_lib/proposeDecision.ts:236` | todo |

### Батч 6 — интерфейс

| № | Находка | Где | Статус |
|---|---|---|---|
| 8 | Кнопка отправки включается по общему счётчику, а выключается по счётчику активной категории | `src/ui.ts:770,777` против `src/ui.ts:522` | todo |
| 15 | У типографики нет фильтра по статусу, экспорта, печати и счётчика «обработано» | `src/ui.ts:982,1942`, `src/ui.html` | todo |
| 16 | Первая строка выделяется автоматически — один клик пишет решение по невыбранной строке | `src/ui.ts:2466` | todo |

### Батч 7 — идентификаторы (отложено)

| № | Находка | Где | Статус |
|---|---|---|---|
| 14 | 32-битный `stableHash` служит вечным идентификатором решения и подписью в реестре | `src/lib/colorUtils.ts:113` | отложено |

Замена схемы обнулит все накопленные решения и подписи — это миграция, а не
правка. Возвращаться, когда реестр вырастет до десятков тысяч записей, и
делать с версионированием идентификатора.

## Открытые продуктовые вопросы

Задаются в начале соответствующего батча, чтобы решение не забылось.

| Батч | Вопрос | Решение |
|---|---|---|
| 1 | Модель доступа к бэкенду: А — убрать отправку из плагина; Б — ограничить частоту и белый список; В — авторизация через GitHub | — |
| 2 | Решение применили в макет: считать закрытым или всё равно отправлять в реестр? | — |
| 3 | Переопределения библиотечных стилей: показывать в таблице или убрать детекцию? | — |
| 5 | Дубликат подписи в реестре: заменять, отклонять или разрешать обе записи? | — |
| 6 | Типографике нужны фильтр и экспорт, или честно пишем «только для цветов»? | — |
| 6 | Автовыделение первой строки: оставить или требовать явного клика? | — |

## Замечено попутно

Сюда пишем то, что нашли вне своего батча, чтобы не отвлекаться.

- Версия `0.1.1` проставлена только в собранном архиве релиза; коммитом в
  репозитории не зафиксирована. Поправить при следующем настоящем релизе.
- `styleId` библиотечного стиля — это `node_id` из REST, а не идентификатор
  стиля (`src/lib/figmaStylesRestApi.ts:232`). Работает за счёт запасного
  матча по `key`, но название вводит в заблуждение.
