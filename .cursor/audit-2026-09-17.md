# Аудит репозитория — 2026-09-17

Read-only аудит. Ветки не переключались, коммиты не создавались,
существующие файлы не изменялись. Создан только этот файл.

---

## Часть 1. Протокол `skills/_shared/repo-operating-protocol.md`

**Файл не существует. Процитировать его правила невозможно.**

Проверено тремя способами:

| Проверка | Результат |
|---|---|
| `ls skills/_shared/repo-operating-protocol.md` | `No such file or directory` |
| `find . -name "*operating-protocol*"` | ничего не найдено |
| `git log --all --oneline -- "*repo-operating-protocol*"` | пусто — файл никогда не коммитился ни в одну ветку |

Единственный файл с «protocol» в имени — `skills/_shared/no-hardcode-color-protocol.md`,
и он посвящён запрету хардкода цветов, а не git-операциям и не internal-файлам.

Правил «про backup перед destructive git-командами» и «про internal-файлы»
в репозитории нет — ни в этом файле, ни в `CLAUDE.md`, ни в `git-workflow.md`
(последний описывает только команды синхронизации памяти скиллов, типы
коммитов, структуру `memory/` и онбординг).

Пересказывать содержимое несуществующего документа я не стал.
Если такой протокол должен существовать — его нужно написать, и тогда
`CLAUDE.md` стоит дополнить ссылкой на него, как это сделано для
`token-rules.md`, `platforms.md` и `git-workflow.md`.

**Замечание по фактической практике этой сессии:** backup перед
`git checkout --` был сделан не по протоколу из репозитория, а по общему
правилу осторожности с необратимыми операциями. Копия отброшенной правки
лежит во временной папке сессии (вне репозитория) и исчезнет вместе с ней.

---

## Часть 2. Причина порчи `token-comparator-typography-feature.md`

**Версия «облачная синхронизация» не подтверждается.** Все четыре проверки
против неё.

| Проверка | Результат | Вывод |
|---|---|---|
| `xattr -l` файла | только `com.apple.provenance` | Атрибутов облачных провайдеров нет: ни `com.apple.fileprovider`, ни `com.google.drivefs`, ни `com.dropbox` |
| `df -h ~/Projects/aid` | `/dev/disk3s5` → `/System/Volumes/Data` | Локальный APFS-том. Не сетевой и не облачный маунт |
| `ls -la ~/Projects/` + `pwd -P` | `/Users/desexpert/Projects/aid` | В пути нет `Google Drive`, `Dropbox`, `CloudStorage`. Symlink-индирекции нет — реальный путь совпадает с логическим |
| `stat -f "%Sm"` | `Sep 17 18:51:52 2026` | **Показывает не время порчи, а время моего `git checkout`** — он перезаписал файл. Исходный mtime повреждённой версии утрачен |

### Что это значит

Механизм «два клиента облака разошлись и слепили конфликтную версию»
исключён — репозиторий лежит на локальном диске без каких-либо облачных
агентов над ним.

### Наблюдение, требующее проверки человеком

Соседние каталоги в `~/Projects/` — это **git worktree того же репозитория**,
а не независимые копии:

```
/Users/desexpert/Projects/aid                          0a7cc0d [feat/token-comparator-typography]
/Users/desexpert/Projects/aid-claude-test              f35561c (detached HEAD)
/Users/desexpert/Projects/aid-presentbook-tools        09eba93 [feat/presentbook-tools-download]
/Users/desexpert/Projects/aid-token-comparator-v0.1.0  89bbf35 (detached HEAD)
```

Четыре рабочих копии одного репо — среда, в которой ручное копирование
файлов между каталогами и правки несколькими агентами/редакторами
параллельно вполне возможны. Но **прямых доказательств, что порча пришла
именно оттуда, у меня нет**, и подтвердить это постфактум уже нельзя:
повреждённая версия откачена, её mtime затёрт.

Характер порчи (`НАЙДЕН` → `НАИДЕН`, `КРИТИЧНЫЙ` → `КРИТИЧНУГ`,
`Это ломает` → `Есло мает`) — не похож на git-конфликт (нет маркеров
`<<<<<<<`) и не похож на сбой кодировки (кириллица осталась валидной
кириллицей). Больше похоже на посимвольные замены внутри слов при
сохранении общей структуры документа. Источник определить по имеющимся
данным нельзя.

---

## Часть 3. Аудит веток

Дата аудита: 2026-09-17. База сравнения — локальная `main`.

| Ветка | Последний коммит | Сообщение | Слита в `main` | Впереди `main` | Позади `main` |
|---|---|---|---|---|---|
| `archive/main-wip-pre-cleanup-20260909` | 2026-09-08 16:22 | `docs(github-sync-architecture): update to v1.2.0` | ✅ MERGED | 0 | 5 |
| `presentbook/icons` | 2026-09-08 16:22 | `docs(github-sync-architecture): update to v1.2.0` | ✅ MERGED | 0 | 5 |
| `feat/presentbook-tools-download` | 2026-09-09 20:47 | `feat(presentbook): add Tools hub and Token Comparator download` | ✅ MERGED | 0 | 1 |
| `cursor/figma-comparator-clean` | 2026-09-04 17:04 | `fix(comparator): guard against missing response.headers in Figma sandbox` | ✅ MERGED | 0 | 24 |
| `cursor/figma-token-comparator-github-sync` | 2026-09-04 15:43 | `fix(comparator): guard against missing response.headers in Figma sandbox` | ❌ **NOT MERGED** | **72** | 28 |
| `cursor/propose-decision-endpoint` | 2026-09-07 10:03 | `feat(registry-api): add human-readable PR review projection for proposed decisions` | ✅ MERGED | 0 | 12 |
| `cursor/token-comparator-v0.1.0` | 2026-09-08 18:30 | `chore(comparator): add reproducible v0.1.0 packaging` | ✅ MERGED | 0 | 3 |

### Выводы

**Одна ветка требует решения:**

`cursor/figma-token-comparator-github-sync` — единственная не слитая,
и при этом на **72 коммита впереди `main`** при 28 позади. Последний
коммит — 2026-09-04, то есть ветка стоит около двух недель. Её содержимое
в `main` не попало. Перед любой чисткой веток нужно понять, что в этих
72 коммитах: уникальная работа или параллельная история, уже
воспроизведённая в `main` другими коммитами (сообщение последнего коммита
дословно совпадает с `cursor/figma-comparator-clean`, которая слита —
это признак, что ветки могли расходиться от общей точки).

**Шесть веток слиты полностью** (0 коммитов впереди `main`) и являются
кандидатами на удаление:
`archive/main-wip-pre-cleanup-20260909`, `presentbook/icons`,
`feat/presentbook-tools-download`, `cursor/figma-comparator-clean`,
`cursor/propose-decision-endpoint`, `cursor/token-comparator-v0.1.0`.

**Осторожно при чистке:** `feat/presentbook-tools-download` **занята
worktree** `/Users/desexpert/Projects/aid-presentbook-tools`. Удалить её
обычным `git branch -d` не получится, пока worktree существует —
сначала `git worktree remove`.

Текущая ветка на момент аудита — `feat/token-comparator-typography`
(в аудит не входила, в таблице отсутствует).

---

## Что НЕ делалось в рамках этого аудита

- ветки не переключались и не удалялись
- worktree не трогались
- коммитов не создавалось, `git add` не выполнялся
- в рамках Частей 1–3 существующие файлы не изменялись
- создан один новый файл: `.cursor/audit-2026-09-17.md`

---

## Состояние рабочего дерева на момент аудита

Рабочее дерево **не чистое**. `git status --porcelain`:

```
 M .claude/space-context.md
 M skills/_shared/cursor-component-pipeline-context.md
 M skills/_shared/platforms.md
 M skills/_shared/semantic-color-tokens-guide.md
 M skills/_shared/token-comparator-release-guide.md
 M skills/_shared/token-rules.md
 M skills/_shared/tracks-2-3-draft-notes.md
?? .cursor/audit-2026-09-17.md
?? skills/_shared/ds-component-spec.md
```

### Происхождение изменений

**Внесено копированием из `~/Downloads` в начале этой сессии (6 файлов):**
`.claude/space-context.md`, `skills/_shared/cursor-component-pipeline-context.md`,
`platforms.md`, `semantic-color-tokens-guide.md`,
`token-comparator-release-guide.md`, `token-rules.md`.

Копирование перезаписало **уже закоммиченные** файлы более новыми версиями
из `~/Downloads`, из-за чего возник незакоммиченный diff. Ещё два
скопированных файла — `semver-guide.md` и `github-sync-architecture.md` —
оказались побайтово идентичны версиям в коммите и diff не дали.

**Существовало до сессии (2 файла):**
`skills/_shared/tracks-2-3-draft-notes.md` (modified) и
`skills/_shared/ds-component-spec.md` (untracked, никогда не коммитился).
К копированию отношения не имеют.

### Что с этим делать — решение за человеком

Изменения из `~/Downloads` не отревьюены построчно. Прежде чем коммитить
их в `feat/token-comparator-typography`, стоит просмотреть
`git diff skills/_shared/` — с учётом того, что ровно в этом репозитории
уже обнаружена порча текста неизвестного происхождения
(см. Часть 2), содержимое файлов из `~/Downloads` доверия по умолчанию
не заслуживает.
